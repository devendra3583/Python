"""
Q. WAP to search for a file in cwd after searching in that directory and all its sub-folders
Input: filename i.e. file.txt
Output: exact path of that file and all path if multiple files(in this case a list of all paths)

For eg-if input is "file.txt" and "D:\" then output will be:
Output:
D:\movies\file.txt
D:\Python\file.txt
Display all paths
"""
#Ans:


